# QQ 机器人操作手册

> 适用：`D:\Agent Space\NapCatShell` 全部件。设计细节见 PLAN.md，本手册只管**怎么用、怎么调、坏了怎么查**。

---

## 1. 日常使用

### 启动
双击桌面 **`启动QQ机器人.bat`** → UAC 提权点"是" → 出现「QQ机器人控制器」窗口 → 输入：

```
start
```

确认提示 `y`（⚠️ **会关闭所有 QQ 进程，包括你的主号**，注入完成后主号可手动重新登录）。

启动成功的标志：控制台显示 `NapCat 已上线：IV (3776580862)`。

### 停止
控制器里输入 `stop`（只停机器人+ NapCat），或 `quit` 退出控制器时会问你是否保留机器人。

### 直接点 X 关窗口
**点 X = 自动执行 stop**。看门狗（watchdog.ps1）会在 2 秒内发现控制器死亡，兜底杀掉 bot 和 NapCat/QQ。不需要你做任何事。

### 最小化到托盘
控制器里输入 `tray` → 控制台窗口消失，右下角托盘出现图标（**Win11 可能被收进托盘溢出区，点任务栏 `^` 小箭头找**）：
- **双击图标** = 恢复窗口
- **右键 → 停止并退出机器人** = 等价 stop + 退出控制器

桌面上永远只会有这一个窗口（或一个托盘图标）。

---

## 2. 控制器命令速查

| 命令 | 作用 |
|---|---|
| `start` | 启动（杀所有 QQ → NapCat 注入 → bot 上线 → 武装看门狗） |
| `stop` | 停止 bot + NapCat/QQ |
| `restart` | = stop + start |
| `status` | 状态总览：进程/模式/模型/缓冲/费用/表情库/最近错误 |
| `log` | bot.log 最后 30 行 |
| `err` | bot.log 里最近的 WARN/ERROR |
| `tray` | 最小化到系统托盘 |
| `quit` | 退出控制器（会问你是否保留机器人） |
| `help` | 帮助 |

---

## 3. QQ 私聊主控命令

用你自己的号（2337529577）**私聊**机器人号 IV（3776580862）发送：

| 命令 | 作用 |
|---|---|
| `/闭嘴` | 机器人只看不说（缓冲照常积累），@也不回 |
| `/说话` | 恢复 |
| `/状态` | 私聊返回运行状态（模式/模型/费用/缓冲/表情库） |
| `/重载` | 热重载 config.json + persona.md，**改配置不用重启** |
| `/人设` | 查看当前完整人设提示词 |
| `/表情` | 表情库统计 + 最近入库 10 个 |

> 私聊别人发来的消息机器人一律不理，只有你的号能指挥它。

---

## 4. 常用调试

### 看它在干嘛
```
status          # 控制器内
```
或直接看文件 `state\status.json`（每 10 秒刷新）。

### 看日志
- `bot.log` —— 全量运行日志（UTF-8）：启动、决策、表情入库、报错都在这
- 控制器 `log` / `err` 命令 = 快速查看

### 看它发了什么
`logs\sent\YYYY-MM-DD.jsonl` —— 每天一个文件，每行一条 JSON：时间、类型（text/meme）、内容。启动时自动删除 7 天前的。

### 看它的"脑子"
- `state\context.json` —— 75 条滚动消息缓冲 + 滚动摘要
- `state\memory.md` —— 长期记忆（≤800 字，摘要时自动维护）
- `state\ledger.json` —— 费用账本

---

## 5. 调参（config.json）

改完让机器人生效的方式：**QQ 私聊发 `/重载`**（不用重启）。

| 参数 | 默认 | 说明 |
|---|---|---|
| `behavior.silenceMinMsgs` | 2 | 主动插嘴门槛：累计 N 条消息+静默后才考虑发言 |
| `behavior.silenceMs` | 20000 | 静默多久触发插嘴决策 |
| `behavior.cooldownMinMs/MaxMs` | 5s~60s | 每次发言后的随机冷却 |
| `behavior.waitMinMs/MaxMs` | 1s~10s | 回复前的拟人等待 |
| `behavior.bubbleGapMinMs/MaxMs` | 0.5s~2s | 多气泡之间的间隔 |
| `behavior.bubbleMaxChars` | 40 | 单气泡长度上限 |
| `behavior.bufferSize` | 75 | 消息缓冲条数 |
| `behavior.summaryEvery` | 75 | 每 75 条新消息滚动摘要一次 |
| `behavior.atThrottlePerPerson` | 10 | 每人 30 分钟内 @ 超过此次数则冷却 30 分钟 |
| `llm.primary` / `llm.fallback` | DS V4.1 Flash / LongCat-2.0 | 模型切换 |
| `llm.maxTokens` | 10000 | 单次回复 token 上限。是上限不是消费，计费按实际 token；**别低于 800**——V4.1 Flash 的思考 token 也占额度，300 时会出现 `空响应(finish=length)` |
| `cost.monthlyBudgetUSD` | 60 | 月度预算（仅展示用，硬护栏见下） |
| `cost.window5hHalveAtUSD` | 7 | 5 小时窗口花费到 $7 → 候选发言减半 |
| `cost.window5hAtOnlyAtUSD` | 9.5 | 到 $9.5 → 只回 @（5h 窗口硬顶 $12） |
| `cost.dailyCallCap` | 200 | 每日 LLM 调用上限，到顶只回 @ |
| `meme.maxCount` | 500 | 表情库上限，超出淘汰最旧 |
| `sentLog.retainDays` | 7 | 发送存档保留天数 |

**嫌话多**：调大 `silenceMinMsgs` / 调大 `cooldownMaxMs` / 调大 `silenceMs`。
**嫌话少**：反向调。

### 切换模型
DS V4.1 Flash 优惠期结束后想省钱：`llm.primary` 改成 `longcat-2.0`，`/重载` 即生效（每月 $15→含在 $60 内，LongCat 无峰时加价）。主模型连续失败 3 次会自动切 fallback，日志可见。

---

## 6. 人设调整

编辑 `persona.md`（顶部有注释），然后私聊 `/重载`。改语气用词、背景、行为规则都在这一个文件里。改坏了私聊 `/人设` 能看到机器人当前实际用的是什么。

---

## 7. 表情包库

- **自动收集**：群里有人发图 → 自动下载 → V4.1 Flash 多模态打标 → 是表情包才入库（普通照片跳过、敏感图直接丢弃）
- **位置**：`memes\` 目录，`memes\index.json` 是索引（文件名=含义_slug+md5）
- **手动加图**：直接把图片文件丢进 `memes\`，并在 `index.json` 的 `memes` 数组加一条 `{"file":"文件名","md5":"随便","meaning":"含义","emotions":["情绪"],"ts":0}`
- **手动删除**：删文件 + 删 index.json 里对应条目
- 机器人发言时最多参考最近 40 个表情

---

## 8. 故障排查

| 现象 | 排查 |
|---|---|
| 机器人完全不理人 | ① `status` 看是否"已闭嘴"（私聊 `/说话`）② `err` 看报错 ③ 群消息数没到触发门槛属正常，@它测试 |
| 只回 @ 不主动说话 | `status` 看模式——可能触发了预算护栏（5h 花超 $9.5 或当日 200 次） |
| NapCat 掉线 | **v1.8 起控制器自动处理**：每 60s 探测 OneBot，连续 3 次失败自动重启 NapCat 层（响铃提醒）；30 分钟内重启 3 次仍失败或需要扫码时会响铃 + 打印 WebUI 调试地址，人工扫码即可。`status` 里可看"存活监控"是否运行中 |
| 提示端口占用 | 有残留进程：任务管理器杀 node.exe（命令行含 bot.cjs 的）或先 `stop` |
| 控制器打不开第二个 | 单实例锁（TCP 3211），找到已开的那个用，或 `stop` 后重开 |
| 表情不收 | 看 `bot.log` 里"打标"相关 WARN；敏感图/普通照片是故意跳过的 |
| 日志偶发 `空响应(finish=length)` | maxTokens 被思考 token 吃光；确认 config `llm.maxTokens` ≥800（现行 10000）。偶发一次无害，重试会成功 |
| 输入 `tray` 没反应 | 托盘图标可能在 Win11 溢出区（`^` 箭头里）；确认没有则把 bot.log 发艾薇 |
| 回复风格跑偏 | 私聊 `/人设` 确认当前提示词 → 改 persona.md → `/重载` |
| 风格复读 | `state\context.json` 的 `recentOwn` 会拦截复读；若仍明显，截图发艾薇调提示词 |

**日志看不懂**：把 `bot.log` 最后几十行发给艾薇。

---

## 9. 文件地图

```
NapCatShell\
├─ 启动入口：桌面 启动QQ机器人.bat → controller.cjs
├─ bot.cjs            机器人本体（隐藏进程）
├─ controller.cjs     CLI 控制器（就你看到的窗口）
├─ watchdog.ps1       看门狗（X 兜底，隐藏运行）
├─ tray.ps1           托盘（tray 命令启动）
├─ persona.md         人设提示词（改了 /重载）
├─ config.json        所有可调参数（改了 /重载）
├─ secrets.json       API key（绝不外发/不入 git）
├─ bot.log            运行日志
├─ state\             status/context/ledger/memory + watchdog.disarm
├─ logs\sent\         发送存档（按天，7 天自动清）
├─ memes\             表情包库 + index.json
├─ bot-legacy.cjs     旧版 ACP 机器人（归档，勿动）
└─ NapCat 本体文件（napcat.mjs/launcher 等，勿动）
```

端口占用：3000（OneBot HTTP）、3210（bot webhook）、3211（控制器锁+托盘命令）。

---

## 10. 注意事项

- **start 必杀所有 QQ**——主号会被踢下线，注入完成后手动重登即可，机器人号不受影响
- **机器人运行期间尽量不要在本机登录主号 QQ**（2026-09-21 疑似因此触发 bot 实例被踢）；要看主号用手机
- NapCat 自身日志已开启（fileLog），掉线原因可查 NapCat 日志文件；看不懂就发给艾薇
- 成本护栏是自动的，烧钱异常时它会自己降级（候选减半 → 只回 @），不用盯着
- API key 在 `secrets.json`，换 key 只改这个文件然后 `restart`
- 机器人不会开机自启，每次想用就双击桌面 bat
- 全部组件出错时：控制器 `restart`；还不行就把 bot.log 发给艾薇
